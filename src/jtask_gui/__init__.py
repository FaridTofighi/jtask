"""jtask-gui — a professional PyQt6 desktop front-end for the jtask core.

The GUI imports the framework-agnostic ``jtask`` package (jalali math,
Taskwarrior client, report shaping) and never shells out to ``task`` or does
calendar arithmetic itself.
"""

__version__ = "0.1.0"
